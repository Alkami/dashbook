name: example
description: Dashbook example

version: 1.0.0+1

publish_to: none

environment:
  sdk: ">=3.3.4 <4.0.0" # dart version
  flutter: ">=3.19.6"

dependencies:
  alkami_core_dependencies:
    hosted: https://dart.cloudsmith.io/alkami/flutter/
    version: ^4.0.0

  dashbook:
    path: ../

  flutter:
    sdk: flutter

dev_dependencies:

flutter:
  uses-material-design: true
